import { SubTitle } from '@/components/DesignSystem';
import LayoutSubWorkToTopic from '@/components/LayoutSubWorkToTopic';

export default function Middleware() {
  return (
    <LayoutSubWorkToTopic title='Middleware' href='/work/nextjs' hrefName='Next.js'>
      <SubTitle>My implementation of Next.js Middleware </SubTitle>
    </LayoutSubWorkToTopic>
  );
}
