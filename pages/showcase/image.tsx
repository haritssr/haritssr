import { Title } from '@/components/DesignSystem';
import Layout from '@/components/Layout';

export default function Image() {
  return (
    <Layout browserTitle='Image' description='Nextjs Image components'>
      <Title name='Image Components' />
    </Layout>
  );
}
